# Game-Structure-Generator
A small Python utility that generates a folder and file structure from a simple YAML definition. It is particularly useful for quickly bootstrapping new projects (e.g., game projects with consistent layouts).
